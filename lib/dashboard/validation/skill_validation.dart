import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class SkillValidation extends StatelessWidget{
  const SkillValidation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Skill Validation'),
      ),
      body: const Center(
        child: Text('Skill Validation'),
      ),
    );
  }

}